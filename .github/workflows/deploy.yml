name: Deploy DAGs to MWAA

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync DAGs to MWAA S3
        run: |
          aws s3 sync dags/ s3://${{ secrets.MWAA_S3_BUCKET }}/dags/ --delete

      - name: Sync requirements.txt
        run: |
          aws s3 cp requirements/requirements.txt s3://${{ secrets.MWAA_S3_BUCKET }}/requirements.txt
